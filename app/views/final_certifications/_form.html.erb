<%= form_for(@final_certification) do |f| %>
  <% if @final_certification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@final_certification.errors.count, "error") %> prohibited this final_certification from being saved:</h2>

      <ul>
      <% @final_certification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :final_grade_id %><br>
    <%= f.number_field :final_grade_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
